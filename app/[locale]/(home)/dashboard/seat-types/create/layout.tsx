import React from "react";

export const metadata = {
    title: "Dashboard : Create Provinces"
};

export default function Layout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
