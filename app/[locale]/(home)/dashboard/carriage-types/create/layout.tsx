import React from "react";

export const metadata = {
    title: "Dashboard : Create Carriage"
};

export default function Layout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
